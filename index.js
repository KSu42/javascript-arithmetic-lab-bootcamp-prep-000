// Basic math functions

function add(a, b) {
  return (a + b)
}
function subtract(a, b) {
  return (a - b)
}
function multiply(a, b) {
  return (a * b)
}
function divide(a, b) {
  return (a / b)
}

// Math + Assignment functions

function inc(n) {
  return (n++)
}
